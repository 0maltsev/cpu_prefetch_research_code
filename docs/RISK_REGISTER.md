# Risk Register

Qualitative likelihood is `UNKNOWN` until implementation or platform evidence exists. An `OPEN_BLOCKER` risk stops the stated gate; it is not a request to invent a default.

| ID | Risk and consequence | Gate exposed | Likelihood | Impact | Control or mitigation | Trigger/evidence | Owner | Status |
|---|---|---|---|---|---|---|---|---|
| R-001 | Normative artifacts contradict or omit a scientific decision; implementation would silently redesign the study. | Any affected phase | Unknown | Critical | Stop affected work; record exact conflict; obtain append-only protocol amendment and new hashes. | Traceability or review finds incompatible obligations. | Protocol owner | Open stop condition |
| R-002 | Source tree lacks Git metadata, weakening source-repository revision identity. | Reproducibility | Certain for current import | Medium | Preserve resolved source identity, UTC import time, byte sizes, every SHA-256, and source-declared hash verification. Do not invent a commit. | `git` metadata absent at import. | Repository maintainer | Accepted limitation |
| R-003 | Repository source is redistributed despite the owner's no-license decision. | Distribution / packaging | Possible | Critical | ADR-0021, no `LICENSE` or SPDX grant, package notice, and separate dependency-license inventory. Independent queue mode and the third-party queue-source prohibition remain in force. | Copy/distribution is proposed without a superseding owner license grant. | Repository owner | Controlled; distribution not authorized |
| R-004 | Accepted C++ atomic envelope later fails release/acquire, lock-free, alignment, or progress obligations on the eligible platform. | Phase 5 queue acceptance / eligible stand | Unlikely in tested development matrices; stand unknown | Critical | ADR-0024 proof; compile/runtime pointer-width/lock-free probes; explicit layout; model, phase-suspension, sanitizer, stress, and dual-disassembler gates; repeat runtime facts on stand. | Failed proof/test/probe or unsupported atomic. | Queue/correctness owner | Stage 5 local evidence controlled; eligible-stand repetition remains open |
| R-005 | General abstraction adds virtual dispatch or treatment-dependent work to the hot path. | Pre-pilot | Unlikely in reviewed queue/package core | High | Separate final adapters and concrete package policies; static emitter; Q14 GNU+LLVM ten-shape combined call-graph audit and call mutant; rerun strict audit after the physical emitter is accepted. | Generated code contains dispatch/branch/unexpected call. | Benchmark owner | Static combined shapes controlled; strict emitter-bound release open |
| R-006 | Physical raw format loses fields/precision, is not stable, or couples logical semantics to one codec. | Pre-pilot/storage | Unlikely in tested software; release drift possible | Critical | Q9/ADR-0032 plus the Stage 11 literal-ID codec, independent Python goldens, strict decoder, semantic mapping, corruption/version tests, and dual-disassembler audit retain the logical codec seam. | C++/Python mismatch; missing logical/raw field; format ID or bytes drift. | Data/storage owners | Stage 11 software controlled; requalify every exact release |
| R-007 | Preallocated buffer or durable storage is insufficient; samples are lost or hot-path I/O is introduced. | Pre-pilot/every run | Unknown on concrete run plan/stand | Critical | Stage 11 implements exact checked formulas, prepared private mappings, sticky fail-on-overflow, no-allocation tests, source separation, and writer codegen; Phase 16 must bind exact horizons, available bytes/reserve, page residency, and real domains without compression credit. | Capacity proof fails; one domain only; buffer overflow or worker-reachable I/O/copy/compression. | Storage/custody owners | Software behavior controlled; operational capacity/residency/domain proof remains open blocker |
| R-008 | Clock is not monotonic/stable across selected cores, a syscall/source fallback occurs, or boundary code moves/strengthens queue ordering. | Phase 8 / pre-pilot / every block | Unknown on eligible pair | Critical | D-009 reader/conversion/boundaries/evaluators and uncorrected diagnostics are implemented; fake-clock/cross-thread/failure tests and GNU+LLVM source/disassembly rules with six mutants pass. Exact full-count and affinity inputs cannot be confused with short diagnostics. | Regression/equal/cost/skew/drift limit failure; source/syscall/migration; moved read or added fence; explicit pair/evidence absent. | Repository/timing/platform/code-generation owners | Stage 8 software controlled; explicit pair, traced vDSO, full dynamic and before-block evidence remain open blockers for measurement |
| R-009 | Requested hardware-prefetch state differs from actual state or only a subset changed. | Pre-pilot/every whole plot | Unknown | Critical | Stage 9 keeps requested/apply/verified fields separate, rejects missing/stale/mismatched readback, and retains evidence IDs; exact vendor manual/fields, privileged authority, regular/pointer probes, and partial-state label remain mandatory. | Capability is mapping-unresolved; verification failed/unknown/mismatched; probe absent. | Platform operator | Software failure path controlled; exact vendor mapping/probes remain open blocker |
| R-010 | Affinity, shared-cache relationship, producer-home pages, or private-buffer homes drift. | Pre-pilot/every run | Unknown | Critical | Stage 9 parses CPU/core/package/NUMA/cache topology and rejects sibling/near/far/memory-policy mismatches; exact thread/address residency must still be verified before/during/after, with migration invalidating the run. | CPU/page migration, stale snapshot/epoch, topology or policy mismatch. | Platform owner | Request/topology controls implemented; actual stand threads/pages remain open blocker |
| R-011 | Linked node cycle is regular/short-period, undermining the permitted pointer-dependence interpretation. | Matrix freeze/every run | Unknown | High | Stage 6 freezes deterministic `C+1` permutation and reports raw deltas/checksums; treatment-blind pre-freeze seed progression and physical page-frame gate remain mandatory. | Stride/modal/period/distinct-line/checksum gate fails. | Workload owner | Construction controlled; qualifying seed/page evidence open |
| R-012 | RNG, permutation, or namespace behavior changes across builds/platforms or collides between roles. | Pre-pilot/block freeze | Unlikely in tested Stage 6/7 matrices; frozen stand inputs unknown | Critical | ADR-0025/0026 suite IDs, separated purpose labels, known-answer and cross-compiler vectors; Stage 7 adds `arrival-schedule`, exact mapping, explicit role/common-family validation, and goldens. | Golden mismatch, namespace collision, or unversioned transform. | Reproducibility owner | Stage 7 implementation controlled; concrete lifecycle namespace/seed freeze remains open |
| R-013 | Compiler removes/moves immutable record loads or checksum update, changing the consumer action. | Pre-pilot | Unlikely in reviewed Stage 6 body | Critical | ADR-0027 fixed mixer, known-answer checksum, source-linked dual-disassembler review/mutant; repeat for final timestamped worker boundaries. | Expected load/update/boundary absent or branch introduced. | Benchmark/correctness owner | Stage 6 body controlled; combined worker open |
| R-014 | Producer/consumer reconciliation uses repeating record index as identity, joins wrong rows, or computes latency before audit. | Pre-pilot/post-run | Unlikely in tested Stage 12 path | Critical | Stage 12 builds accepted producer order, joins exact k-th ordinals, checks Stage 6 mapping and k-th index, derives only after an all-stream pass, and fault-injects first/internal/last loss, duplicate, reorder, identity, mapping, count, and timestamp errors. | Any mismatch yields joined rows or a passed audit. | Reconciliation owner | Run-level implementation controlled; requalify release and integrate immutable store by Phase 16 |
| R-015 | A correctly reconciled `FULL` is misclassified as invalid or selectively repeated. | Confirmatory validity | Unlikely in tested software | Critical | Independent validity/zero-loss states; Stage 10 one attempt; Stage 12 valid-`FULL`; Stage 14 replacement requires `INVALID` plus exact invalidating failure/authority/budget. | Failure/replacement record cites `FULL` alone. | Controller/analysis owner | Software enforcement controlled; operational audit remains |
| R-016 | Genuine low `N_eff` triggers extension/repetition or deletion. | Confirmatory validity | Unlikely in tested software | Critical | Stage 12 retains `VALID` and fails only tail; Stage 14 rejects low-tail valid runs as replacement evidence; Stage 15 retains the run and returns a blocked primary-analysis result without extension, filtering, or replacement. | Collection changes after observing low effective count. | Analysis/controller owner | Software enforcement controlled through Stage 15; operational audit remains |
| R-017 | Invalid cell is repaired in place, incomplete block enters primary inference, or replacements continue until success. | Confirmatory collection | Unknown operationally; unlikely in tested planner | Critical | Stage 14 proves exact original/replacement products and lineage, exposes only full-block generation, requires invalid-run/failure/authorization/budget agreement, rejects branches/role/seed reuse, and stops unresolved at the cap. | Cell rerun, reused seed/ID, role change, branch, or cap exceeded. | Replacement authority | Software controlled; concrete budget/authority and append-only custody remain blockers |
| R-018 | H3 validation or H1/H2 outcomes leak before permitted state, biasing selection or sizing. | Confirmatory access | Unknown operationally; unlikely in tested ledger | Critical | Stage 14 validates exact chronology/block sets, complete hashed `TRAINING_OPEN` precision inputs/counts, predecessor hashes, roles/namespaces, six contexts, segregated authorities/approved overlaps, outcome-blind sizing/amendments, and fail-closed access decisions. Storage/custody must enforce negative access. | Unauthorized read/summary/log/access, incomplete or wrong transition freeze, or collapsed principals. | Custodian/access auditor | Software ledger controlled; operational technical sealing remains blocker |
| R-019 | H3 sizing depends on selected candidates or observed validation outcomes. | Pre-confirmatory | Unknown operationally; unlikely in tested software | Critical | Stage 14 freezes fixed 270 training and 540 validation precision inputs before selection. Stage 15 consumes only those immutable family-width curves, selects across all ten candidates in six contexts, and evaluates only the registered 54 selected-minus-alternative contrasts after authorized unseal. | Sizing inputs contain selection/validation results or only 54 comparisons. | Freeze authority/statistics owner | Software ordering and family controls pass; concrete treatment-blind precision evidence remains open blocker |
| R-020 | H1/H2 are combined into an incorrect 27-member family or resample events as independent runs. | Analysis | Unlikely in tested software | Critical | Stage 15 pins separate seven- and twenty-member registries and resamples complete temporal blocks with separate two-sided max-T calculations; known-null, known-shift, rank, and deterministic-bootstrap tests pass. | Family/resampling validation mismatch. | Statistics owner | Stage 15 software controlled; requalify every analysis profile change |
| R-021 | Matrix zero-loss case uses zero pilot full events as zero risk or ignores total planned exposure. | Pre-confirmatory | Unknown | Critical | Simultaneous per-cell bounds; all scheduled exposure; frozen union-bound calculation, `pi_matrix`, and global rule. | Missing exposure/bound or zero upper bound inferred from zero events. | Calibration/statistics owner | Open blocker |
| R-022 | `delta_star`, counts, capacities, rates, `d2`, horizons, seeds, thresholds, or budgets are fabricated. | Architecture through confirmation | Unknown | Critical | Keep values open; accept only evidence-backed ADR/freeze records; automated scan/review for unsupported constants. | Unreferenced concrete platform/pilot value appears. | Phase owner/protocol reviewer | Open blocker |
| R-023 | Warm-up reset remaps/retouches data, leaves warm-up events, or is incorrectly described as cold start. | Pre-pilot/every run | Unknown | High | Stage 10 preparation/warm-up/reset validators require stop/drain/barrier, exact ring/linked origin, zero logical state, unchanged allocation/mapping/home/permutation/payload identity, and no allocation/regeneration/remap/retouch. | Phase report mismatch or mapping/checksum change. | Controller owner | Fake-backed contract controlled; concrete queue reset and per-run evidence remain pre-pilot gates |
| R-024 | Environmental carryover or disturbance exceeds treatment-blind limits. | Pre-confirmatory/every run | Unknown | High | Randomized/counterbalanced whole plots, frozen recovery and acceptance rules, disturbance records, sensitivity only where permitted. | Threshold failure or recovery evidence absent. | Platform/controller owner | Open blocker pending pilot |
| R-025 | Performance counters are treated as causal or unavailable events are inferred. | Diagnostics/analysis | Unknown | High | Counters remain separate matched diagnostics; record encoding/domain/skid/errata; randomized latency contrasts carry inference. | Causal claim rests on correlation or missing counter. | Analysis owner | Open |
| R-026 | Synthetic/development-machine results are mistaken for empirical evidence. | Reporting | Possible | Critical | Stage 15 accepts only its explicit synthetic input profile, labels machine reports `SYNTHETIC_KNOWN_ANSWER_ONLY`, labels human reports `SYNTHETIC FIXTURE ONLY` and states that they contain no empirical findings, and binds every source/config/output hash. Production-artifact admission requires a separately accepted later profile and Phase 16 evidence. | Result/recommendation lacks authorized confirmatory source IDs or removes the synthetic label. | Publication reviewer | Synthetic software path controlled; final claim/provenance review remains open |
| R-027 | Stage B/C work consumes effort or substitutes for incomplete Stage A. | Planning | Unknown | High | Explicit exclusion from initial architecture/plan; require amendment; scope/file audit. | B/C production component or replacement claim appears. | Project owner | Open |
| R-028 | Tooling drifts or a clean/offline reproduction fails. | Pre-pilot/release | Possible | High | ADR-0022 constrained series, fail-closed configure checks, source/package hashes, dependency licenses, generated metadata, pinned CI action, and clean local dual-toolchain verification. Re-run on provisioned self-hosted runners and exact-lock measured releases. | Version/inventory mismatch, network fetch, runner failure, or metadata mismatch. | Build owner | Stage 3 locally controlled; external CI/measurement lock later |
| R-029 | Schema validation is mistaken for semantic validation. | Every data gate | Possible | Critical | ADR-0023/0034/0040/0041 separate imported Draft validation, strict loading, record-local rules, Stage 12 run relationships, Stage 14 factorial/access/replacement graphs, and Stage 15 analysis admission. Schema-valid semantic negatives are mandatory. | Cross-record/arithmetic defect passes because JSON shape is valid. | Validation owner | Software layers controlled through Stage 15; every later consumer must invoke all applicable gates |
| R-030 | Artifact correction overwrites raw evidence or creates branched/ambiguous lineage. | Post-run/access | Unknown | Critical | Immutable raw sources, new derived artifacts, `supersedes_id`, predecessor-hash/lineage checks, storage permissions. | Source hash changes or multiple unauthorized successors appear. | Data custodian | Open |
| R-031 | Accepted implementation bundle drifts without superseding ADR or requalification. | Stage 4 onward | Possible | Critical | Bind D-001..019 and D-028..033 to ADR-0007..0029; fail on unrecorded stack, topology, license, build-policy, algorithm suite, model, queue/workload/schedule source, or compatibility changes. | Build/source differs from accepted ADRs without supersession. | Repository owner / maintainer | Controlled open risk |
| R-032 | Current FastFlow provenance is mistaken for the exact 2010 paper artifact or its then-applicable license. | Queue provenance | Unlikely after controls | High | Independent implementation is accepted; machine records mark exact artifact unresolved/current project only; source hash checker rejects FastFlow implementation references; reuse needs a superseding ADR. | Source/review cites or copies current FastFlow implementation text. | Queue provenance owner | Stage 5 provenance control passes; remain vigilant on source changes |
| R-033 | Canonical JSON coerces protocol integers through IEEE-754 and changes artifact identity. | Stage 4 identity layer | Unlikely after controls | Critical | `JCS-I64-v1` preserves signed/unsigned 64-bit integers; C++ and independent Python fixtures cover 2^53 and integer boundaries; parser rejects duplicate keys, non-finite and out-of-domain numbers. | Round trip changes an integer or cross-tool canonical bytes differ. | Data integrity owner | Stage 4 controlled; requalify every parser/serializer change |
| R-034 | Standard-library RNG/distribution or platform `log` produces build-dependent schedules. | Pre-pilot | Unlikely in the implemented path | Critical | Stage 6 freezes Philox; Stage 7 uses offline Python Decimal80 with exact bit consumption, arithmetic, unit, rounding, encoding, overflow, hashes, and C/_pydecimal/C++ goldens. | Cross-build vector mismatch, unspecified distribution algorithm, platform-libm call, or unapproved suite. | Reproducibility owner | Stage 7 implementation controlled; requalify exact measured release |
| R-035 | Exact consumer mixer is selected for convenience or after performance observation, altering measured work. | Pre-pilot | Unlikely after owner acceptance | Critical | ADR-0027 owner acceptance, fixed formula/vectors/generated-code signature, and no treatment outcomes in selection. | Formula/body differs without supersession and requalification. | Consumer/protocol owner | Stage 6 controlled; requalify final worker |
| R-036 | Development host or candidate inventory is mistaken for an eligible near/far NUMA stand. | Phase 9 / pre-pilot platform readiness | Possible | Critical | Development inventory remains software-only. Q13 selects topology-evidenced `(0,1)` near and `(0,26)` far, but clean inventory remains `INVENTORY_ONLY_NOT_QUALIFIED`; admission separately requires current platform verification, clock, atomic/layout, and address-residency hashes. | Pair selection or admission shape is cited as dynamic qualification; platform record lacks fresh pair-specific readback. | Platform owner | Pair selected; dynamic qualification remains `OPEN_BLOCKER_BEFORE_PILOT` |
| R-037 | Measurement process holds privilege or validation access, allowing timed interference or H3 leakage. | Stage 3 architecture / confirmation | Unknown | Critical | Stage 9 exposes an injected external actuator, separate verifier, and no production mutating backend; runbook forbids measurement-process privilege. Separate validation custodian/storage still needs operational negative access proof. | Measurement binary receives control capability or validation access; actuator/verifier authority collapses. | Security owner / custodian | Platform software boundary controlled; operational authority/custody open blocker |
| R-038 | Accepted physical encoding or copy policy is treated as proven beyond its evidence boundary. | Stage 11 / pre-pilot | Possible | High | Stage 11 cross-decoder, corruption, checked-budget, codegen, no-replace, fault, and recovery tests pass; documentation distinguishes these local software facts from concrete capacity, residency, domain, custody, and operational recovery proof. | Local directories or synthetic counts are cited as production-domain/capacity evidence; bytes drift under accepted IDs. | Storage owner | Local implementation controlled; Phase 16 operational proof remains open |
| R-039 | A record is treated as globally valid after only structural and record-local validation. | Every artifact/access gate | Possible | Critical | Stage 12 resolves run relationships; Stage 14 resolves seed catalogs, active pool, precision, access, lineage, and budget; Stage 15 refuses analysis unless immutable hashes, reconciliation evidence, active-block completeness, replacement lineage, and role-specific access grants all pass. Final production acceptance still requires concrete evidence. | A consumer bypasses a gate-specific pass or treats a synthetic/unresolved graph as final. | Validation, reconciliation, access, and analysis owners | Integrated synthetic path controlled through Stage 15; production Phase 16 invocation/evidence remains blocker |
| R-040 | A validator silently imposes one summary-reason priority when several independent confirmatory blockers coexist. | Phase 12 / reporting | Unlikely in tested pre.2 path | High | D-031/pre.2 requires an exhaustive unique UTF-8-ordered array and `BLOCKED_MULTIPLE`; all-five and malformed-order tests pass; pre.1 remains version-bound. | A final artifact omits an applicable blocker or emits priority order. | Protocol/statistical/validation owners | Controlled by ADR-0034 and Stage 12; requalify every version/rule change |
| R-050 | A protocol document upgrade accidentally changes frozen workload/schedule derivation bytes or mixes pre.1/pre.2 evidence. | Every version/reproducibility gate | Possible | Critical | ADR-0034 separates current document version from ADR-0025/0029 pre.1 derivation-domain labels; old/new golden bytes, dual-snapshot hashes, exact schema requirements, and mixed-graph rejection are tested. | Philox key/deadline golden changes under the old suite ID, or a Stage 12 graph contains both versions. | Protocol/reproducibility/validation owners | Controlled locally; converters require new derived identity and explicit lineage |
| R-041 | Single-tool generated-code evidence is mistaken for ADR-0016 dual-disassembler acceptance. | Every release | Possible | Critical | Strict queue, workload, timing, and storage checks fail closed without either tool; CI requires both; evidence binds source/binary/rules/body/full-disassembly hashes and negative mutants. | A release is accepted without a GNU+LLVM passing report and review. | Build/code-generation owners | Stage 11 local instances controlled; re-run and review every changed exact release |
| R-042 | Independently authored queue source changes without provenance/refinement requalification. | Every queue source change | Possible | Critical | Per-queue SHA-256 inventory, required provenance target, ADR-0024, targeted behavior/sanitizer/codegen matrix. | Source hash mismatch or provenance record edited without matching proof/review. | Queue/provenance owners | Controlled by fail-closed checker; requalify on change |
| R-043 | OpenSSL or independently implemented Philox behavior drifts while retaining the same suite ID. | Every workload-source/dependency change | Possible | Critical | OpenSSL 3 inventory/license constraint, independent known-answer vectors, purpose-domain tests, cross-compiler matrices, and immutable suite ID; change the suite ID for incompatible bytes. | HMAC/Philox/checksum golden mismatch or unrecorded crypto dependency change. | Reproducibility/build owners | Stage 6 instance controlled; requalify on change |
| R-044 | Decimal context, operation order, rate canonicalization, or deadline/hash bytes drift while retaining the accepted schedule suite ID. | Every Stage 7 source/tool/version change | Possible | Critical | ADR-0029 implementation uses an explicit local context, integer-only inputs, direct/integrated goldens, C versus pure-Python decimal parity, C++ decoder parity, fail-closed versioning, exact runtime derivation record, and immutable suite IDs. | Any golden/hash mismatch or implicit context/binary-float use. | Reproducibility/data-integrity owners | Stage 7 instance controlled; requalify every source/runtime/release change |
| R-045 | A partial platform apply leaves the stand in an unknown or unrestored state and later work treats it as default. | Platform admission/every whole plot | Unknown on stand | Critical | Stage 9 requires fresh pre-state, stops on first failed apply, restores successful changes in reverse order, retains every restore failure, and marks the manifest ineligible. Quarantine until independent restoration readback passes. | Missing pre-state; apply failure; inverse failure; absent post-restore readback. | Platform operator / security owner | Software orchestration tested; exact stand inverse operations and exercise remain open blocker |
| R-046 | Rich Stage 9 evidence is squeezed into the immutable platform schema, dropped, or confused with its logical summary. | Platform freeze/artifact ingest | Possible | High | Emit a versioned rich canonical evidence manifest and a separate exact imported-schema projection; both are content-hashed and unknown versions fail closed. | Extra fields appear in imported record, or apply/restoration/provenance evidence is missing from referenced artifacts. | Platform/data-integrity owners | Stage 9 emitters and schema-loading tests control local representation; store linkage remains later |
| R-047 | Termination publication, barrier origin, or drain races lose backlog, misclassify failure, or hang a worker. | Every measured run | Possible before integrated evidence | Critical | ADR-0031 dedicated lock-free u32 release/acquire word, one-origin start barrier, second-acquire race attribution, explicit watchdog failure, backlog/empty/delayed-worker/100-history tests, and no retry API. | Accepted count differs from consumed after drain; consumer exits before acquired finish plus empty; watchdog silently reruns; failure phase depends on race. | Controller/queue-correctness owners | Stage 10 fake-backed behavior controlled; final storage/package specialization, codegen, stand lock-free/layout, and watchdog values remain open |
| R-048 | A crash-recovery path creates new evidence, overwrites a published object, or promotes an unverifiable staging file. | Post-run recovery | Unlikely in tested local backend; operational environment unknown | Critical | Recovery reopens an exact hashed run directory in recovery-only mode, forbids new publication, requires expected byte count and SHA-256, uses no-replace linking, and independently rereads the result; fault/restart/mismatch tests pass. | Recovery API publishes a new object; wrong hash/size promotes; an existing object changes. | Storage/recovery/custody owners | Local crash-restart behavior controlled; exact-release operational exercise remains Phase 16 |
| R-049 | Observation-buffer pages are first-touched by the controller or migrate, invalidating worker-private placement assumptions. | Pre-pilot/every run | Unknown on stand | Critical | Constructors reserve without touching; the already-affined owner must explicitly initialize every reserved byte before release; no append is accepted before preparation. Phase 16 must verify exact address residency before/during/after. | A controller-side first touch occurs; preparation is omitted; private pages land or migrate to the wrong node. | Storage/platform/controller owners | Stage 11 API/tests control preparation order; stand residency proof remains open blocker |
| R-051 | Service-rate calibration silently assumes a Gaussian mean, extends until enough favorable valid runs exist, or excludes a low valid throughput. | Stage 13/pre-confirmatory | Unknown on stand; unlikely in tested evaluator | Critical | The implemented D-035 evaluator requires the prospective 60-cell plan, authority/budget evidence, exact context equality, exact rational throughput, 59 valid results, and a per-run immutable status/source decision; it rejects unplanned top-up. Synthetic minimum/zero/58/59/context tests pass. | Estimator target/plan changes after access; a valid minimum is omitted; `mu_cell` is emitted from fewer than 59 valid results. | Protocol/statistical/calibration owners | Software controlled; exact execution plan and stand evidence remain open blockers |
| R-052 | Matrix feasibility assumes independent events, converts zero observed FULL to zero risk, omits candidate/cell multiplicity, or tunes one favorable cell. | Stage 13/pre-confirmatory | Unknown on stand; unlikely in tested evaluator | Critical | The implemented D-036 offline evaluator uses prospective run IDs and per-probe validity/source decisions, exact run clusters, candidate-bound `180*5`, positive finite all-zero upper bounds, all exposures, and the complete common ladder; invalid/missing/unplanned probes, zero-offered runs, wrong thresholds, and confirmatory access reject. | `p_U=0` from finite all-zero data; event-binomial likelihood without approved dependence evidence; missing cell/scale; cell-specific rate. | Protocol/statistical/block-planning owners | Software/profile controlled; probe evidence, `Rtotal`, and schedules remain blockers |
| R-053 | Ring `d2` is chosen from treatment effects, one hardware state, central summaries, or an instrumented boundary that does not correspond to advancing slot demand. | Stage 13/R2 freeze | Unknown on stand; unlikely in tested evaluator | Critical | The calibration-only queue observer brackets the exact acquire, preallocated traces exclude FULL/empty from advancement cadence, and the evaluator requires R0, both workers/states, exact tails, 59 valid results, common distance, and cap/collapse checks. | Distance varies by H state or outcome; wrong demand operation; missing worker/state/context; cap collapse is accepted. | Protocol/statistical/queue/timing owners | Software controlled; stand-derived context evidence remains blocker |
| R-054 | A provisional calibration result is treated as final despite missing stand identity, exact plan, `Rtotal`, schedules, authority, or a material dependency change. | Stage 13 through confirmation | Possible | Critical | Five strict schemas require owner/authority/budget and per-run decisions where applicable, preserve honest partial states, bind every candidate index/scale, and use canonical source hashes, append-only exclusive publication, and material fingerprint comparison; final matrix evaluation still requires exact exposure. | Placeholder/default field; provisional record cited as freeze; dependency hash changed without invalidation. | Calibration/validation/data-integrity owners | Software/profile controlled; later input/evidence gates remain blockers |
| R-055 | Decimal precision or operation order drifts while retaining the Stage 13 upper-bound profile ID. | Every Stage 13 evaluator/runtime change | Possible | Critical | ADR-0039 freezes guard160/upward steps/output80 and a Decimal240 reference; direct unequal-weight vectors and an enclosure comparison run in `calibration-check`. | A boundary vector changes, the precision-160 result is below the reference, or binary floating point enters the bound. | Statistical/reproducibility owners | Controlled by profile/version tests; supersede and recalculate on any incompatible change |
| R-056 | The offline analysis changes estimands, resampling units, multiplicity families, binary64 serialization, or access admission while retaining the Stage 15 profile ID. | Analysis/reproducibility/reporting | Possible | Critical | ADR-0041 binds exact inverse-ECDF estimands, complete-block Philox bootstrap/max-T, 7/20/270/540/54 registries, access chronology, binary64 bit encoding, zero-self-hashed canonical reports, and a synthetic-only input profile. Deterministic reruns and prohibited-input mutations are mandatory. | Same profile ID yields different bytes; event/run resampling appears; a family changes; sealed/mixed/incomplete input is admitted. | Statistical/reproducibility/access owners | Stage 15 synthetic profile controlled; production input adapter and exact release require Phase 16 requalification |
| R-057 | A stand-preflight bundle is mistaken for a measurement release or silently embeds platform/pilot authority. | Stage 16 transfer / pre-pilot | Unlikely after Stage 16 controls | Critical | ADR-0042 versions the preflight-only profile; manifest explicitly denies pilot/confirmatory authority; example inputs are nonauthoritative/null; the only executables are smoke and read-only inventory/preflight; internal verifier checks the boundary. | Bundle contains a measurement path or non-null platform value, or its manifest authorizes collection. | Build/platform/protocol owners | Stage 16 profile controlled; every superseding bundle requires full requalification |
| R-058 | Passing component or stale combined assembly is misrepresented as the final emitter-bound measurement-worker audit. | Pilot release | Unlikely after D-047 | Critical | D-047 audits ten static combined operation shapes with both disassemblers, exact instruction vectors, source ordering/targets, and four mutants; the candidate requires schema-v2 `PASS`, mapping identity, exact clean binary/source/rule hashes, and all other strict reports. | A release uses a stale/partial report, wrong mapping/schema, unresolved marker, or report not bound to its exact binary. | Benchmark/build/code-generation owners | `CONTROLLED_OPEN_REQUALIFY_ON_DRIFT` |
| R-059 | Bundle bytes, source state, build provenance, or dependency/license inventory drift during transfer or extraction. | Stage 16 transfer / every release | Unlikely in tested path | High | Deterministic normalized archives, Git/source-state binding, complete internal SHA-256 inventory, external archive sidecar, SPDX SBOM, no-license notice, append-only output, repeat-build hash equality, clean extraction, and nonprivileged self-tests. | Repeat builds differ; sidecar/internal inventory fails; undeclared file appears; source revision or protocol metadata disagrees. | Build/reproducibility owner | Stage 16 instance controlled; repeat for every changed source/build/profile |
| R-060 | RAID members or two paths on one mounted filesystem are mislabeled as two independent D-020 durable artifact domains. | Pre-pilot storage admission | Certain on the currently observed stand layout | Critical | Hashed `findmnt`/`lsblk`/`df`/`mdstat` evidence records one suitable mounted namespace on `/dev/md3`; its RAID1 redundancy is retained as a fact but explicitly rejected as two copy destinations. Require a second named domain with a distinct failure/control/custody boundary and an exact recovery/hash-readback exercise. | Both durable copy IDs resolve to `/dev/md3`, one mount, or the same custody/failure boundary. | Storage/custody owners | `OPEN_BLOCKER_BEFORE_PILOT`; provision/evidence a second domain |
| R-061 | A self-declared admission record, field-only validation, or stale artifact path is treated as authority to start workers. | Runner admission / pre-pilot | Unlikely after Q14 implementation | Critical | ADR-0044 keeps ticket construction private to the combined current trust-anchor and regular-file SHA-256 path; exactly 21 immutable eligible current-binding kinds are required, including separate software-prefetch mapping and phase authority; dirty builds, symlinks, mismatch, and absence fail closed; the CLI exposes no execution command. | A ticket is constructible without reading evidence bytes, an unbound/mutable/ineligible record passes, or the admission CLI starts measurement. | Controller/security/custody owners | Admission software controlled; operational evidence and execution authorization remain open |
| R-062 | A generic, conditional, SSH, or root approval is treated as an omnibus authorization for dependent qualification, calibration, pilot, or confirmatory phases. | Pre-Stage-17 governance / every execution gate | Unlikely after Q14 policy | Critical | Accepted ADR-0044 through ADR-0046 require non-collapsible local-policy, exact hash-bound Q15 qualification, and dependency-ready Q16a through Q16d phase records. The schema/semantic suite rejects omnibus, wildcard/latest/unresolved, overlapping-authority, predecessor-free, confirmatory, top-up, repair, and retry forms. | A later phase starts from Q14, shell/root access, `latest`, wildcard targets, unresolved inputs, or predecessor-free approval. | Protocol/statistical/platform/controller/security/custody owners | Policy controlled; Q15/Q16 records unavailable until exact prerequisites exist |
| R-063 | A physical software-prefetch instruction drifts to a compiler default, changing write/read intent or retention semantics. | D-044 release / Stage A treatment integrity | Unlikely after D-047 | Critical | ADR-0047 fixes `X86-64-PREFETCHW-PREFETCHT0-v1`, uses an explicit instruction template, requires both owner CPUs to expose PRFCHW, records a typed mapping artifact, and requires exact GCC/Clang plus GNU/LLVM vectors and wrong-intent/duplicate mutants. H0/H1 hardware control remains separate. | Any selected worker lacks PRFCHW, R1/R2/L1 contains the wrong mnemonic/site/count, a fallback occurs, or mapping/report identity drifts. | Protocol/platform/workload/code-generation owners | `CONTROLLED_OPEN_DYNAMIC_Q15_EVIDENCE_REQUIRED` |
| R-064 | A processor-speed-dependent poll cap converts a legitimate open-loop idle gap or accepted drain backlog into an invalid run. | Lifecycle / every measured run | Unlikely after ADR-0048 | Critical | Runner/admission v3 removes producer-idle, consumer-empty, and drain poll caps; regression tests cross the former scales; hang containment is external and prospectively authorized. | Any worker failure is triggered only by number of relax polls, or a backlog is abandoned before termination-plus-empty. | Protocol/controller/lifecycle owners | `CONTROLLED_SOFTWARE_REVERIFY_RELEASE`; exact external watchdog open before Q15 |
| R-065 | A broad or wrong-model MSR command changes undocumented state, self-verifies, or cannot restore the candidate. | Q15 hardware-prefetch qualification | Possible until dynamic evidence | Critical | ADR-0049 exposes no arbitrary MSR/mask, preserves bits 63:4, fixes CPUs 0/1/26, requires independent complete readback, two probe families, reverse complete restoration, and quarantine. Fake and schema negatives cover mismatch/failure. | Family/model/CPU/prestate drift; readback uses writer; one probe absent; restore differs; quarantine false on uncertainty. | Platform/protocol/security/audit owners | `OPEN_BLOCKER_BEFORE_Q15_EXECUTION` |
| R-066 | Root or one filesystem is mislabeled as independent qualification authority/custody. | Q15 governance and evidence transfer | Certain until principals are established | Critical | ADR-0050 requires four distinct identities, forbidden-action negative tests, and separate candidate/development domains; authorization v2 rejects overlap and same-domain records. | One credential fills two roles; auditor can mutate; custodian can control; both copies remain on `/dev/md3`; no transfer receipt. | Security/custody/platform/controller owners | `OPEN_BLOCKER_BEFORE_Q15_ISSUANCE` |
| R-067 | One Q15 approval authorizes mutation before the exact prestate exists, or a production control/probe is improvised outside a hash-bound reviewed tool. | Q15 qualification | Reduced locally; operational inputs absent | Critical | ADR-0051 fixes a separate no-measurement tool, exact Q15-R read-only acquisition, then distinct Q15-W bound to immutable prestates. The v1 split schema rejects mutation/readback/inverse/probe defects; blocked preparations cannot validate as authority. The adapter fixes model/MSR/CPUs and exact values. | Prestate is filled after signature; root/manual shell is used; writer and verifier share identity; command/tool hash is absent; Q15-R is treated as Q15-W. | Platform/security/controller/protocol/audit owners | `SOFTWARE_BOUNDARY_CONTROLLED; OPEN_BEFORE_Q15`: clean tool/probes/roles/limits/custody/signatures and dynamic evidence remain mandatory |
| R-068 | A timing effect, multiplexed counter, wrong PMU event, or ad hoc pointer traversal is treated as H0/H1 behavioral qualification. | Q15 qualification | Software mechanism controlled; dynamic evidence absent | Critical | ADR-0052/0053 fix event/traversal/classification/integrity. ADR-0055 implements only raw pinned user-only `0xf824`, one reset and one enable/traversal/disable lifecycle, no retry/fallback, raw counter times/faults, and strict GNU/LLVM traversal plus caller-boundary mutants. ADR-0056 fixes all seven distinct collectors. No execution authority exists. | PMU encoding or scope drifts; `time_enabled != time_running`; elapsed time becomes a threshold; H1 count is nonzero; pointer H0 zero is mislabeled distinguished; a collector is omitted; an unreviewed or unbound executable runs. | Protocol/platform/timing/compiler/security/audit owners | `LOCAL_IMPLEMENTATION_CONTROLLED; OPEN_BEFORE_Q15`: clean hash-bind, exact Q15-R authority, and dynamically collect complete evidence before any Q15-W |
| R-069 | Separate Q15-R H0 and Q15-W H1 invocations silently replace D-052's same-buffer requirement with reproducible bytes or a file-backed remap. | Q15 dynamic implementation and qualification | Software mechanism controlled; operational proof absent | Critical | ADR-0054 implements one session retaining one private anonymous mapping through H0 seal, later H1, and restoration readback. Complete profile/platform/peer/evidence hashes, live buffer hash, expiry, illegal states, disconnect, and terminal failure are fail-closed; partial transitions remain accessible. | H0 and H1 have different mapping lifetimes; a shared/file mapping replaces private anonymous memory; a session resumes after expiry/disconnect; Q15-W mutates a sealed Q15-R artifact; identical bytes are asserted to mean identical buffer. | Protocol/platform/security/custody/audit owners | `LOCAL_IMPLEMENTATION_CONTROLLED; OPEN_BEFORE_Q15`: clean release, exact session argv/endpoint and OS roles, then authorized dynamic proof; never default to same-bytes |
| R-070 | A generic approval of Q15-R-P1/P2/P3 is mistaken for execution authority, or ad-hoc shell orchestration bypasses the fixed same-buffer/controller graph. | Q15-R preparation and authority | Local mechanism and clean release controlled; operational authority absent | Critical | ADR-0057..0065, authorization-v2, fixed controller, no-authority CLI, and fixed-FD adapter pass fake/structural checks. Q15-R-P3 selects the exact `c8b69ab` release as evidence only; every stand/Q15 authority remains false. | Stand access starts from release-evidence acceptance; CLI refusal is bypassed; a receipt for different bytes is reused; actual account/key/custody/signature evidence or later signed approval is missing. | Protocol/platform/security/controller/custody/audit owners | `LOCAL_RELEASE_SELECTED_NO_AUTHORITY; OPEN_BEFORE_Q15_R`: verify OS/custody/trust setup under separate authority, then later approve exact signed Q15-R |
| R-071 | Accepted setup templates or a verified no-authority archive are mistaken for an executable stand transaction, or unresolved tokens are filled from convenience. | Q15-R operational prerequisite/setup | Controlled by immutable predecessor and accepted versioned successor | Critical | ADR-0061..0065 preserve 20/24/10 transaction identity and all authority false. The six-input predecessor remains hash-bound. Successor v2 resolves only exact release evidence and retains five unresolved groups; 11 combined acceptance/successor negatives reject authority, lineage/release drift, fabricated/missing inputs, transaction loss, and issuance. | Any `@...@` token survives execution approval; root/shared identity substitutes for four roles; secondary domain/prestate is inferred; setup runs before exact authority; quarantine is called full restoration. | Security/platform/controller/custody/audit owners | `RELEASE_EVIDENCE_RESOLVED; BLOCKED_BEFORE_STAND_SETUP`: resolve/hash five external groups, independently review, then separately authorize setup |
| R-072 | An accepted acquisition method or collector binary is treated as external evidence or execution authority, or key/path/prestate values are defaulted before their owners and target evidence exist. | Q15-R-P4 external-input acquisition | Methods and clean release controlled; external values and authority absent | Critical | ADR-0066..0071 keep all literal values null and separate P4-R/P4-K/P5/Q15-R/Q15-W. P4-E selects only exact clean collector bytes. P4-R v2 resolves one release group, preserves 25 commands/limits, and retains seven null inputs; P4-K remains byte-preserved with eight null inputs. | A value becomes non-null without evidence; selected bytes are treated as authority; a stand key is created; a path is inferred; collector runs without exact P4-R; prestate lacks exact argv/raw output/hash; one approval collapses later gates. | Security/platform/release/custody/audit owners | `COLLECTOR_RELEASE_SELECTED_NO_AUTHORITY; BLOCKED_BEFORE_Q15_R_P4_R_P4_K`: separately obtain literal signed authority for each external action |
| R-073 | Stale stand inventory, root/SSH reachability, frozen literal paths, or one omnibus approval is treated as fresh identity and authority to stage or collect; failure cleanup then destroys evidence or touches an operational root. | Q15-R-P4-R identity, staging, and collection | P4-R-I identity/review complete; P4-R-C unissued | Critical | Q15-R-P4-F/ADR-0072..0075 split P4-R-I from P4-R-C. D-099/ADR-0099 binds the pinned host/transport, target signature, four read-only observations, immutable capture/review, one attempt/zero retry, and mandatory stop. P4-R-C retains its separate create-exclusive paths, verification, stop-retain-no-delete, and exact future authority. | D-099 is stretched into P4-R-C authority; identity becomes stale; root is called scientific authority; a destination exists; retry/delete/activation occurs; collector runs without the separately signed gate. | Protocol/platform/security/release/custody/audit owners | `P4_R_I_COMPLETE_VALID; BLOCKED_BEFORE_P4_R_C`: preserve D-099; prepare, sign, and explicitly approve one exact P4-R-C before any staging or collector execution |
| R-074 | Accepted P4-K logical policy/identifiers are mistaken for proof or permission to discover, create, import, copy, use, or disclose private key material, or one approval collapses acquisition, review, installation, and signing. | Q15-R-P4-K signer and trust-anchor evidence | Policy/templates controlled; operational inputs and authority absent | Critical | ADR-0076..0079 select a later new ceremony, logical-only domain/custodian, split P4-K-A/P4-K-R, and fixed authority policy. Original P4-K retains eight nulls; templates retain 13/9 nulls. Acceptance/template checks reject 10/14 authority, evidence, self-authorization, role-collapse, retry, continuation, and issuance mutations. Target key cannot authorize its own creation. | Logical domain is called verified custody; any private key/passphrase/secret path enters repository/stand; bootstrap signer is inferred; P4-K-A auto-continues to review/install/sign; template is called authorization. | Security/custody/audit/protocol owners | `POLICY_ACCEPTED_TEMPLATES_UNISSUED; BLOCKED_BEFORE_P4_K_A`: supply exact non-secret ceremony/custody/bootstrap/public-path/UTC/signature/review inputs, independently review, then separately authorize one action |
| R-075 | A convenient local/stand `ssh-keygen`, invented offline path, chat/root/SSH approval, or the nonexistent target key is treated as exact P4-K-A environment, bootstrap trust, or execution authority; secret inputs then leak into argv/logs/artifacts. | Q15-R-P4-K-A operational input and ceremony | Policy and generic controller controlled; no bootstrap signer, external value, or operational authority | Critical | Q15-R-P4-K-A-D/ADR-0080..0085 require exact offline inventory/tool hashes/network state, encrypted key and uncaptured secret, non-secret custody receipt, create-exclusive public export, a separately governed bootstrap root, fixed no-shell controller policy, one attempt/zero retry, exact UTC/SSHSIG/review, append-only partial evidence, and mandatory stop. ADR-0086 implements only a no-OS-backend admission engine. Seven values remain null; Q4 explicitly blocks. | Negative Q4 is ignored; current host/stand tool is assumed; a private path/passphrase/seed is recorded; target self-signs; bootstrap fingerprint/trust is invented; generic implementation is treated as action authority; retry/cleanup occurs. | Security/protocol/custody/repository/release/audit owners | `GENERIC_CONTROLLER_IMPLEMENTED_NO_BOOTSTRAP_SIGNER_NO_AUTHORITY; BLOCKED_BEFORE_P4_K_A`: separately govern a bootstrap root, collect/review exact external evidence, then separately authorize one action |
| R-076 | A generic controller or blanket chat delegation is mistaken for genesis authority, or a bootstrap root is created on the repository host/stand without real owner identity, witness, offline custody, recovery, and public-trust evidence. | Bootstrap governance-root establishment | Superseded before acceptance by D-093; downgrade risk transferred to R-077 | Critical | ADR-0086 has no OS backend and requires complete signed external admission. The immutable D-087..D-092 proposal records the stronger controls that D-093 explicitly waived for genesis. D-093 separately bound and authorized exactly one action; D-094 separately records activation; signing and later authority remain absent. | D-093/D-094 are stretched beyond their exact scopes; root activation is called current signing/P4-K/Q15 authority; append-only evidence or stop gates are ignored. | Protocol/security/custody/repository/audit owner under D-093 role-collapse waiver | `SUPERSEDED_CONTROL_PROFILE; D094_ROOT_ACTIVE_UNUSED`: retain this history; manage accepted downgrade under R-077; require a separate prospective signing/action gate |
| R-077 | The D-093 single-owner, development-host, unencrypted, no-recovery bootstrap root is impersonated after host/account compromise or is irrecoverably lost; role collapse also removes independent detection and review. | D-093/D-094 active bootstrap root and every later authorization that may trust it | Accepted critical downgrade; root active; used for terminal D-095, completed D-096, and completed D-097 | Critical | Create-exclusive mode `0600` private files outside the repository; public fingerprint/hash evidence; private contents never read or hashed by repository tools; append-only lifecycle/action records; suspected compromise or loss stops every dependent authorization; every signature remains a separate exact gate. D-095 through D-097 signature bytes/dispositions are retained. These controls do not eliminate the accepted risk and there is deliberately no recovery copy. | Unexpected private-file metadata/owner/path change; host or account compromise; lost storage; fingerprint/public mismatch; any spent signature is reused as later authority; lifecycle stop is ignored. | Single protocol/security/custody/audit owner under accepted role-collapse waivers | `CRITICAL_ACCEPTED_OPEN; ROOT_ACTIVE_USED_THREE_TIMES`: signatures are spent for exact transactions; reverify state before any separately authorized future signature; compromise/loss requires terminal stop and new root |
| R-078 | A terminal one-shot credential transaction is retried, repaired, cleaned up, or promoted after partial failure, hiding the failed lineage or bypassing a fresh authorization. | D-095 terminal failure and D-096 successor | D-095 realized and contained; D-096 completed as a distinct action | Critical | D-095 partial tree remains append-only and its target private path absent. ADR-0096 used a new transaction/path, corrected regression-tested tool, fresh authorization/signature, and create-exclusive action; evidence checker binds both lineages and rejects retry/P4-K-R authority. D-097 later proceeded only through its own authorization. | Either tree changes/disappears; old signature is reused; a v1 key appears; D-096 is described as retry/repair; later action is falsely attributed to D-095/D-096 authority. | Single security/custody/release/audit owner under accepted waiver | `D096_AND_D097_COMPLETE_AS_DISTINCT_TRANSACTIONS`: preserve every lineage; no evidence grants retry, repair, P5, or downstream authority |
| R-079 | Creation of the unencrypted single-owner P4-K target key is mistaken for independent review, installation, trust activation, unrestricted signing authority, or permission to probe private material. | D-096 target key and later uses | Critical downgrade accepted; public review complete; exactly one D-099 signature spent | Critical | ADR-0097 binds public-only review with no private probe. ADR-0099 authorizes and records exactly one later target-key SSHSIG for P4-R-I; its signature hash and stop boundary verify. No installation occurred and no unused downstream authority exists. | The one D-099 use is stretched to P4-R-C/P5/Q15; review is called independent despite role collapse; private material is probed; another signature is made without exact approval. | Single protocol/security/custody/release/audit owner under waiver | `D099_SIGNATURE_SPENT; CRITICAL_WAIVER_OPEN`: preserve all evidence; every later target-key use requires new canonical bytes and separate exact authority |
| R-080 | Reviewed public trust evidence, P4-R-I identity, or a partially resolved P5 preparation is mistaken for permission to stage, collect, invent custody, install trust, or execute setup. | D-098/P5 preparation and P4-R/P5 gates | Two P5 public groups and fresh P4-R-I identity resolved; P4-R-C/P5 authority absent | Critical | D-098 retains operational root/secondary custody/current prestate null. D-099 resolves only fresh read-only identity/review and explicitly stops before P4-R-C. Checkers reject fabricated inputs, premature issuance, installation, staging/collector/P4-R-C/P5/Q15 scope, and automatic continuation. | Identity output is called full prestate; two directories on one filesystem are called independent; template is called authorization; staging/collection/setup begins before exact P4-R-C/P5 approval. | Security/platform/release/custody/audit owners | `BLOCKED_BEFORE_P4_R_C_AND_P5`: separately prepare/sign/approve collection, review its output, resolve the P5 successor, then separately approve exact P5 |
| R-081 | D-099 collapses operator, custodian, and auditor into one owner and uses root SSH transport, reducing independent detection and increasing host/account impersonation or misuse impact. | D-099 P4-R-I evidence and any proposed reuse | Accepted critical downgrade; one action completed | Critical | Exact target SSHSIG, pinned Ed25519 host key, fixed transport public identity/options, four read-only commands, bounded evidence, one attempt/zero retry, create-exclusive local capture, public review, and no-mutation/P4-R-C stop. These controls constrain but do not eliminate the accepted risk. | Any key/host/account compromise, output tampering, role-separation claim, retry, evidence overwrite, or reuse of D-099 as later authority. | Single protocol/platform/security/custody/audit owner under waiver | `CRITICAL_ACCEPTED_OPEN; D099_COMPLETE_STOPPED`: retain evidence append-only; reassess freshness and issue a prospective exact gate before P4-R-C or any identity refresh |
| R-082 | P4-R-C implementation or blanket approval is mistaken for action authority; predecessor evidence is overwritten; an unregistered remote-shell/runtime path or namespace mutation escapes the fixed boundary. | D-100..D-103 and D-104 P4-R-C preparation | Accepted local implementation; action/runtime identity unresolved | Critical | ADR-0100..0103 bind single-owner labeling, exact five-file predecessor inventory, create-exclusive outputs/failure retention, fixed local `shell=False` OpenSSH allowlist, pinned identities, one attempt/zero retry, exact parent rule, separate result review, and no-action schemas. Full fake graph/failure injection and shell-command negatives pass. The null remote-runtime acceptance blocks execution. | Implementation is called authorization; remote Python/`dd`/tar compatibility is assumed; target key is used; outputs are overwritten; review is fabricated; retry/delete/activation or later work occurs. | Protocol/platform/security/repository/custody/audit owner | `IMPLEMENTED_LOCAL_NO_ACTION_AUTHORITY; BLOCKED_ON_REMOTE_RUNTIME_EVIDENCE_BEFORE_P4_R_C`: freeze clean bytes, prospectively close runtime identity/risk, then separately issue/sign/review/approve one exact action | New clean executor/schema and prospective ADR for any command/runtime/path/tool/review change; every action needs new canonical bytes/hashes and exact approval; retain failed bytes |
| R-083 | Remote Python, `dd`, tar/compression modules, or dependency observations are inferred from Ubuntu/path conventions, or read-only runtime collection is treated as P4-R-C authority. | D-105..D-108 runtime compatibility gate | Proposed exact bundle; all choices/action inputs null | Critical | Decision SHA `6d753cee...` binds clean D-104 bytes and recommends a fixed pinned, signed, read-only, one-attempt/zero-retry capture; explicit single-owner labeling; canonical complete evidence; separate review; and a clean successor. Schema/checker mutations reject selection, authority, source, prerequisite, and statement drift. | Runtime hashes are defaulted; partial/self-asserted evidence is accepted; target key or stand is used before a later exact gate; capture auto-continues to successor/P4-R-C; module/path drift is ignored. | Platform/security/repository/release/custody/audit owner | `PROPOSED_EXACT_ACCEPTANCE_REQUIRED; KEY_STAND_AND_P4_R_C_AUTHORITY_NONE`: exact decision acceptance before local collector implementation; later signed action and reviewed evidence before successor | Prospective ADR and clean collector for accepted choices; new capture/successor for every runtime/command/path/host drift; retain partial bytes |
| R-084 | The Stage 17 single-owner role collapse removes independent misuse/tamper detection, or is incorrectly extended to Stage 18 sealing/access; alternatively, a per-observation decision chain is recreated and deadlocks pilot entry. | Stage 17 pilot governance and Stage 18 access boundary | Accepted for pilot; ten external inputs absent | Critical | ADR-0104 limits collapse to pilot, requires explicit non-independent labeling, one finite adjacent graph, immutable D-099..D-108 hashes, no automatic advance, and unchanged Stage 18 chronology. ADR-0105 separates immutable definitions from append-only state and rejects chronology weakening. No legacy release metadata closes an input without real bytes. | A review claims independence; another decision-only observation gate is added; a state skips evidence; pilot authority opens validation/H1H2; confirmatory roles/chronology are collapsed. | Protocol/pilot/security/custody/audit owner | `PILOT_DOWNGRADE_ACCEPTED; PREPARED_EXTERNAL_INPUTS_REQUIRED; STAGE18_STRICT`: resolve catalog inputs before pilot and require separate Stage 18 readiness/sealing authority |
| R-085 | Mutable checklist state, a journal fork/replay, or a fabricated artifact ID/hash is accepted as operational evidence; an unavailable pilot-candidate archive is then treated as a resolved release. | Stage 17 state persistence, evidence resolution, and release custody | Implementation corrected; external evidence absent | Critical | ADR-0105 freezes graph/catalog/schema/genesis hashes, requires one-record append-only snapshots and exact predecessor prefixes, recomputes repository regular-file bytes, verifies external locator bytes and receipts, and applies an additional fixed caller-supplied archive/sidecar contract to S17-EXT-006. Disk tests reload all records and reject 15 mutations including duplicate transition inputs and `DOES-NOT-EXIST`/all-`a` SHA-256. | A legacy status field is called current; a record path is absent/symlinked; a receipt's artifact bytes disappear/drift; two snapshots share a predecessor; sequence/predecessor/graph/catalog hashes change; integration is skipped. | Repository/pilot/release/custody/audit owner | `CONTROLLED_SOFTWARE; EXTERNAL_EVIDENCE_REQUIRED`: actual state is `PREPARED`, 0/10 resolved; stop on any missing byte, drift, fork, replay, expiry, or Stage 18 widening |
| R-086 | Integrity-valid but semantically meaningless evidence, stale/caller-selected time, mutable inputs, dead runtime identity, non-durable one-shot state, or a live child authorizes or outlives Stage 17 work. | Stage 17 semantic evidence admission and action readiness | Local implementation controlled; real evidence absent | Critical | ADR-0115/policy v11 binds all ten verifiers and loaded schemas/runtime, derives release/runtime class from fully verified bytes, requires EXT002/003-to-EXT006 equality, sealed trust/signature/worker/request consumption, streamed typed output admission, actual UTC, durable markers, supervised Q15 recovery, and PID-namespace-safe quiescence. | Any input passes without role/schema/source-byte semantics; authority selects trust/executable; runtime or schema drifts; exit zero substitutes for typed result; Q15-W lacks restoration; a process family survives; pilot authority crosses Phase 18. | Repository/pilot/platform/release/custody/audit owner | `CONTROLLED_SOFTWARE; REAL_INPUTS_REQUIRED`: all ten checked-in resolutions remain absent and no action is ready |
| R-087 | A fixed Stage 17 action is a stub, accepts a dynamic experiment definition, consumes the wrong release, or publishes an incomplete/misattributed result. | Q15/Q16/pilot controller and worker | Local implementation controlled; stand execution untested | Critical | The production runner exposes only Q15-R, Q15-W, Q16a, Q16b, Q16c, and the blinded pilot through a sealed-fd dispatcher. Q15 uses the EXT002/003 observed worker and Q16/pilot require byte-equal EXT006 release identity. Q16a retains raw ring-demand ticks; Q16b performs continuously-ready bounded capture; Q16c/pilot consume frozen schedules through the static package runner and private raw streams. Controller completion requires typed action-specific result, full output set, hashes, lineage, and restoration/quarantine semantics. | A production CLI enables a fake backend or arbitrary argv; a package/action branch is dead; planned attempts are reported as actual offered events; raw trace/streams are absent; result lineage or fixed output set is not revalidated. | Repository/platform/calibration/pilot/storage owner | `CONTROLLED_LOCAL; STAND_EVIDENCE_REQUIRED`: next action is the ordered stand handoff, not empirical execution from synthetic fixtures |

## Review cadence

Review this register at each phase acceptance and whenever an ADR, platform record, failure, or amendment changes a trigger. Closing a risk requires linked evidence; confidence or a successful unrelated test is insufficient.
