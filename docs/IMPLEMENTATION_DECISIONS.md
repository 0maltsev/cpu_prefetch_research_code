# Implementation Decision Register

Protocol version: **`2.0.0-pre.1`**

Register date: **2026-08-17**

Stage 2 state: **`AWAITING_OWNER_ACCEPTANCE_AND_PLATFORM_EVIDENCE`**

`ACCEPTED` means an ADR freezes the choice. `PROPOSED` is a recommendation awaiting explicit owner acceptance. `UNRESOLVED` is not a default. `PROTOCOL_FIXED` restates source-of-truth behavior and is never an engineering choice. A selected implementation that changes scientific meaning requires a versioned protocol amendment, not merely an ADR.

## Protocol-fixed boundary

The immutable snapshot fixes Stage A treatments and counts, record semantics, open-loop one-attempt arrivals, timestamp boundaries, queue package behavior, producer/consumer independence, reconciliation identity, equations, quantiles, lifecycle states, partial-failure retention, zero-loss and low-`N_eff` rules, complete-block replacement, H1/H2/H3 analysis chronology, and requested-versus-verified hardware state. Stage B and Stage C remain outside scope. No item below may reinterpret those rules.

## Evidence reviewed for Stage 2

Repository and environment evidence was supplemented by primary/official technical sources. These sources establish candidate feasibility and provenance only; they do not accept a choice or qualify the target stand.

- **Queue paper and artifact search:** M. Torquati, *Single-Producer/Single-Consumer Queues on Shared Cache Multi-Core Systems*, [arXiv 1012.1824](https://arxiv.org/abs/1012.1824) and [University of Pisa TR-10-20](https://calvados.di.unipi.it/storage/paper_files/TR-10-20.pdf). The [author-maintained FastFlow repository](https://github.com/fastflow/fastflow) is an official current project artifact and states dual LGPL-3.0/MIT licensing for current releases. It does not, without an immutable historical mapping, establish the exact source revision or applicable license for the queue algorithms evaluated in the 2010 report. Status is therefore `CURRENT_OFFICIAL_PROJECT_FOUND; EXACT_PAPER_ARTIFACT_UNRESOLVED`. No queue source was downloaded or reused.
- **C++/toolchain feasibility:** the [C++ atomic-order specification](https://eel.is/c++draft/atomics.order) and [generic atomic lock-free rules](https://eel.is/c++draft/atomics.types.generic) make the proposed release/acquire mapping expressible while leaving lock freedom implementation-defined. [GCC 16 changes](https://gcc.gnu.org/gcc-16/changes.html), [Clang 22 release notes](https://releases.llvm.org/22.1.0/tools/clang/docs/ReleaseNotes.html), and [libc++ C++20 status](https://libcxx.llvm.org/Status/Cxx20.html) support a capability probe, not a blanket guarantee.
- **Build/test candidates:** official [CMake Ninja generator](https://cmake.org/cmake/help/latest/generator/Ninja.html), [GoogleTest CMake quickstart](https://google.github.io/googletest/quickstart-cmake.html), [RapidCheck](https://github.com/emil-e/rapidcheck), [AddressSanitizer](https://clang.llvm.org/docs/AddressSanitizer.html), and [ThreadSanitizer](https://clang.llvm.org/docs/ThreadSanitizer.html) documentation. Dependency versions/licenses still require immutable lock records and offline probes.
- **Deterministic/integrity candidates:** [Random123](https://github.com/DEShawResearch/random123) publishes Philox counter-based generators under BSD-3-Clause; [RFC 8785](https://www.rfc-editor.org/rfc/rfc8785.html) supplies JSON canonicalization rules but its I-JSON number model cannot be allowed to coerce the protocol's exact large integers; [OpenSSL EVP SHA-2](https://docs.openssl.org/3.5/man7/EVP_MD-SHA2/) supplies the proposed vetted SHA-256 dependency.
- **Linux platform candidates:** official/man-pages documentation for [`sched_setaffinity`](https://man7.org/linux/man-pages/man2/sched_setaffinity.2.html), [NUMA memory policy](https://www.kernel.org/doc/html/latest/mm/numa.html), [page migration/residency](https://www.kernel.org/doc/html/latest/mm/page_migration.html), [transparent huge pages](https://www.kernel.org/doc/html/latest/admin-guide/mm/transhuge.html), [CPU frequency control](https://www.kernel.org/doc/html/latest/admin-guide/pm/cpufreq.html), [kernel timekeeping](https://www.kernel.org/doc/html/latest/timers/timekeeping.html), and [`CLOCK_MONOTONIC_RAW`](https://man7.org/linux/man-pages/man3/clock_gettime.3.html). CPU-specific HW-PF fields still require the exact target vendor manual, authorization, readback, probes, and rollback evidence.

The observed development environment (Linux 7.0.11 x86-64, GCC 16.1.1, Clang 22.1.6, CMake 4.3.3, Ninja 1.13.2, AMD Ryzen 5 5500U, one NUMA node) is capability evidence only. It is not an eligible near/far stand and supplies no empirical result.

## Accepted architecture decisions

| ID | Classification | Options considered | Selected option | Evidence | Scientific effect | Compatibility effect | Owner | Deadline / gate | Supersession rule |
|---|---|---|---|---|---|---|---|---|---|
| D-021 | Architecture | Interleaved harness; three planes | Three planes and closed prepared run image; [ADR-0001](decisions/0001-plane-separation-and-timed-boundary.md) | Protocol §§6-7, 12, 16, 19-20; explicit Stage 2 instruction | Preserves timed boundaries; no semantic change | Modules exchange prepared images and immutable artifacts; framework-loop policy remains D-004 | Repository owner / implementation maintainer | Accepted before Stage 3 | Later ADR may refine deployment; timed-loop expansion needs protocol amendment |
| D-022 | Data architecture | Embedded physical layout; replaceable codec/store; in-loop schema serialization | Logical model behind codec and artifact-store interfaces; physical format remains open; [ADR-0002](decisions/0002-logical-model-and-replaceable-physical-storage.md) | Data dictionary, schemas, protocol §§16,20; explicit instruction | Preserves exact logical observations | Every codec is explicitly versioned and independently decoded | Repository owner / data owner | Accepted; codec due before pilot | New codec by ADR; logical change needs amendment |
| D-023 | Queue architecture | Normalized abstraction; duplicated drivers; semantic seam with later binding choice | Narrow adapters that preserve and expose package semantics; binding mechanism remains proposed; [ADR-0003](decisions/0003-semantics-preserving-queue-adapters.md) | Protocol §§4-7, Appendix A; explicit instruction | Preserves treatment and linearization identity | Each bound adapter needs its own proof and code evidence | Repository owner / queue owner | Accepted; provenance/binding still block Stage 3 queue work | Binding may be selected by ADR; semantic normalization needs amendment |
| D-024 | Artifact compatibility | Infer format; mutate unversioned format; explicit versions | Protocol/schema, artifact-kind, physical-format, and algorithm-suite identifiers; fail closed; [ADR-0004](decisions/0004-artifact-versioning-and-compatibility.md) | Version record, schemas, data dictionary, protocol §20 | Prevents silent reinterpretation | Unknown major/algorithm IDs rejected; conversions create derived artifacts | Repository owner / data owner | Accepted before Stage 3 | Compatible additions need declaration/tests; incompatible meaning needs new version and possibly amendment |
| D-025 | Integrity / lifecycle | Discard partials; rewrite records; append immutable transitions | Append-only partial failures, audits, corrections, and replacement lineage; [ADR-0005](decisions/0005-append-only-partial-failure-lifecycle.md) | Schemas, access/sealing protocol, protocol §§16,19-20 | Preserves failed evidence and complete-block rule | Store and readers must support incomplete lifecycles | Repository owner / controller owner | Accepted before Stage 3 | Only stronger immutable policy by ADR; destructive loss needs amendment |
| D-026 | Validation | Schema only; combined repairing validator; two fail-closed passes | Draft 2020-12 structure plus independent semantic validation; [ADR-0006](decisions/0006-structural-and-semantic-validation.md) | Seven schemas, data dictionary, freeze checklist | Enforces rather than changes semantics | Stable rule/result versions; validator product is replaceable | Repository owner / validation owner | Accepted; library due Stage 3 | Implementation may change under conformance suite; rule removal needs protocol review |

## Recommendations requiring explicit acceptance before Stage 3

| ID | Classification | Options considered | Selected option or unresolved state | Evidence | Scientific effect | Compatibility effect | Owner | Deadline / gate | Supersession rule |
|---|---|---|---|---|---|---|---|---|---|
| D-001 | Language | C++20; Rust; C with platform atomics | `PROPOSED`: C++20 data plane and controller-facing core | Protocol needs explicit acquire/release, alignment, fixed storage, generated-code review; C++ atomic model and both candidate toolchains support the envelope | Affects generated instructions but does not change semantics if proofs pass | Requires C++20-capable compilers; stable C-compatible/offline artifact boundary preferred | Repository owner / implementation owner | Accept before any production source in Stage 3 | New accepted ADR; semantic changes require protocol review |
| D-002 | Target OS / CPU | Linux x86-64; another evidenced NUMA platform; portable multi-OS | `PROPOSED`: Linux x86-64 only for Stage A; exact eligible CPU/stand remains `UNRESOLVED` | Protocol requires NUMA, affinity, page, clock, frequency, and HW-PF control evidence; local development host is x86-64 but has one NUMA node and is not an eligible stand | Platform is a study scope boundary; requested and verified states remain separate | Non-Linux/non-x86 artifacts unsupported unless a later port ADR and equivalence suite exist | Repository owner / platform owner | OS/ISA before Stage 3; eligible inventory before Phase 9 | Port by ADR; changing study population after freeze may need amendment |
| D-003 | Toolchains | GCC/libstdc++; Clang/libc++; one toolchain only; mixed combinations | `PROPOSED`: GCC 16.x + libstdc++ primary; Clang 22.x + libc++ secondary; no mixed-standard-library link; exact patch versions/flags pinned in Stage 3 | Local capability inventory has GCC 16.1.1 and Clang 22.1.6; official compiler/library documents cover C++20; clean probes still required | Secondary build detects compiler-sensitive UB and codegen divergence; only one frozen release build may produce measurements | Both build matrices must pass; ABI artifacts and C++ objects are not cross-library compatible by promise | Build owner | Family choice before Stage 3; exact pins before pilot | Toolchain change by ADR and full clean-room requalification |
| D-004 | Build / dependencies | CMake+Ninja; Meson; Make; online dependency fetch; vendoring/system packages | `PROPOSED`: CMake + Ninja, locked presets/toolchain files, no network access during configure/build/test, content hashes and licenses for every dependency, no Google Benchmark scientific loop | Recommended baseline; CMake has a first-party Ninja generator; protocol demands reproducible build and provenance | Build system has no scientific effect if generated code and inputs are identical | Minimum versions, lock metadata, offline build, and project license must be frozen | Build owner / repository owner | Accept before Stage 3 foundation | Build ADR and migration evidence; measured binary changes require requalification |
| D-005 | Test frameworks | GoogleTest; Catch2; custom; RapidCheck; libFuzzer; custom stress runner | `PROPOSED`: GoogleTest unit tests, RapidCheck property tests after compatibility probe, CTest orchestration, repository-owned deterministic/stress executables; Google Benchmark only for non-scientific developer microtests | GoogleTest supplies maintained CMake integration; RapidCheck supplies shrinking/property testing but needs a current-toolchain probe | Test tools do not define scientific behavior; randomized tests use recorded test-only seeds | Dependencies need licenses, hashes, offline availability, sanitizer interoperability | Test owner / build owner | Accept before Stage 3 | Framework changes by ADR-equivalent engineering record plus conformance parity |
| D-006 | Process / threads | One process + controller and two workers; separate worker processes; controller service | `PROPOSED`: one unprivileged measurement process, quiescent controller main thread, exactly one producer and one consumer worker; privileged control and validation custody remain out of process | Protocol fixes one producer/consumer and private observations but not deployment; simplest ownership/topology avoids IPC in hot path | No new measured work; controller must not touch hot lines during horizon | POSIX thread/affinity model; crash state and handoff interface must be tested | Architecture owner / controller owner | Accept before Stage 3 data-plane design | Process topology change by ADR and failure/cache-ownership revalidation |
| D-007 | Queue provenance / implementation mode | Reuse official code; licensed adaptation; independent implementation | `PROPOSED`: independent implementation from cited paper and frozen protocol; do not import FastFlow queue code. `UNRESOLVED`: exact historical paper-version artifact/hash/license mapping and repository source license | Official Torquati report and author-maintained FastFlow repository exist; current FastFlow is dual LGPL-3.0/MIT, but current repository state does not establish the exact 2010 artifact-to-algorithm/license mapping | Independent implementation must refine exact algorithm/package semantics; it cannot invent behavior | Avoids dependency ABI, but requires clean-room notes, paper mapping, project license, and stronger tests | Provenance owner / repository owner / queue owner | Accept mode/license before Stage 3 entry; blocks queue source in Phase 5 | Accepted provenance ADR; algorithm semantic change needs amendment |
| D-008 | Atomics / memory order / layout | C++ atomics; compiler intrinsics; locks; stronger orders | `PROPOSED`: `std::atomic` pointer handoffs; protocol release stores/acquire loads at slot/link/recycler and termination publication; relaxed access only for thread-owned cursors after written proof; measured cache-line alignment from verified platform, not hard-coded. Required pointer atomic width equals the selected ABI pointer width; the exact termination/control atomic type and width remain unresolved until its representation/proof is reviewed. Compile-time and runtime lock-free probes are mandatory | Protocol Appendix A and termination rules; C++ makes lock freedom implementation-defined and exposes `is_lock_free` | Intended to map fixed happens-before relations; no weaker or alternative scientific order is allowed | Reject platform/build if required atomics are not always lock-free; no claim beyond SPSC try operations and fixed-arena recycler | Queue correctness owner / platform owner | Accept envelope before Stage 3; exact widths/proof before Phase 5 queue acceptance | Mapping changes by ADR and renewed proof; scientific order change is Stage C/amendment only |
| D-014 | Identity / crypto dependency | Custom SHA; platform crypto; vetted library; JSON/CBOR canonicalization | `PROPOSED`: OpenSSL 3 EVP SHA-256 and HMAC-SHA-256; versioned `JCS-I64-v1` canonical records (RFC 8785 sorting/string/whitespace rules plus exact shortest base-10 schema integers, never binary-double coercion) | Protocol fixes SHA-256 but not dependency/canonical bytes; plain RFC 8785/I-JSON numeric limits do not cover all exact protocol integers | Stable identities only; canonicalization must not alter logical values | Cross-tool golden fixtures are mandatory; unknown suite IDs fail closed | Data integrity owner / build owner | Accept before Stage 3 artifact/seed APIs | New algorithm suite/version and converters; existing artifact IDs immutable |
| D-015 | Platform control / authority | Direct privileged benchmark; external helper; operator runbook only | `PROPOSED`: unprivileged measurement process; narrow pre/post-run platform-control service or authorized operator; no privileged calls in timed path | Protocol requires least privilege, readback, evidence, rollback, and separation from validation custody | Prevents control work from entering measurements and preserves requested/verified distinction | Requires Linux-specific adapter, whitelisted operations, audit records, and deployment authority | Platform owner / security owner | Accept boundary before Stage 3; actual authority before Phase 9 | Authority/API changes by ADR and repeated access/rollback tests |
| D-016 | Generated code | Manual spot-check; compiler-specific dumps; dual-tool disassembly rules | `PROPOSED`: hash source-linked release objects/disassembly; inspect with GNU objdump and llvm-objdump; machine rules plus human review per queue/package specialization and negative mutants | Protocol requires boundary, load, checksum, prefetch, dispatch, and polling evidence | Detects compiled deviations from fixed behavior | Tool/output formats must be pinned; signatures are compiler/build specific | Code-generation reviewer / build owner | Accept before Stage 3 test foundation; pass before pilot | Tool migration by ADR-equivalent record and negative-fixture parity |
| D-017 | Sanitizer / correctness acceptance | ASan/UBSan; TSan; model checking; stress only | `PROPOSED`: zero unresolved findings under ASan+UBSan and TSan for both supported toolchain matrices where supported; no unreviewed suppressions; deterministic refinement tests plus recorded-seed long stress; release artifacts only after all relevant gates pass | Repository rule requires targeted tests and relevant sanitizers; compiler docs define available sanitizer constraints | Correctness evidence only; sanitizer performance is never empirical data | Separate instrumented builds; every suppression/tool exclusion needs written rationale and owner | Test owner / queue correctness owner | Accept before Stage 3; thresholds frozen by Stage 4 | Changes require risk review and equal-or-stronger evidence |

## Decisions deliberately left open until platform or pilot evidence exists

| ID | Classification | Options considered | Selected option or unresolved state | Evidence | Scientific effect | Compatibility effect | Owner | Deadline / gate | Supersession rule |
|---|---|---|---|---|---|---|---|---|---|
| D-009 | Clock | Invariant TSC/RDTSCP; `CLOCK_MONOTONIC_RAW`; another proved clock | `UNRESOLVED`; candidate x86-64 abstraction reads integer ticks, with invariant TSC/RDTSCP as primary candidate and `CLOCK_MONOTONIC_RAW` as calibration/reference candidate | Kernel/CPU manuals plus stand-specific skew, drift, serialization, migration, resolution, cost, and generated-code evidence are absent | Clock validity controls every duration and boundary; no fallback may change mid-study | Format records clock/conversion identity; unsupported clock rejects run | Timing owner / platform owner | Select for Phase 8; pass by Phase 16 | Clock suite/version by ADR; changed source requires recalibration and new artifacts |
| D-010 | Physical raw encoding | Fixed binary rows; Arrow/Parquet; another columnar/binary envelope | `UNRESOLVED`; accepted replaceable interface only (D-022) | Protocol intentionally defers format until exact row sizes, corruption behavior, capacity, and cross-decoder evidence exist | Logical data must be unchanged; timed write cost/layout may affect measurement and must be frozen | Format/version/endianness/compression/copy count explicitly recorded | Storage owner | Select for Phase 11; pass by Phase 16 | New format version and converter; never rewrite source |
| D-011 | RNG / seed derivation | Standard-library RNG; PCG; Philox; AES counter; hash-only derivation | `PROPOSED` for pre-pilot approval: Random123 Philox4x32-10 streams; HMAC-SHA-256 domain-separated derivation with length-prefixed UTF-8 labels and fixed big-endian integers; master seed values remain open | Random123 publishes counter-based Philox and BSD-3-Clause terms; protocol requires reproducible namespaces and stable bit streams | Changes ordering/deadline draws but not distributions if frozen prospectively; treatment outcomes cannot select it | Versioned vector suite required across compilers/tools | Reproducibility owner / statistical owner | Algorithm for Phases 6-7; seed values before confirmation | New suite/version only prospectively; existing schedules immutable |
| D-012 | Permutation | Biased modulo shuffle; sort-by-random-key; Fisher-Yates with rejection | `PROPOSED`: Fisher-Yates descending order using a specified 64-bit rejection sampler over the selected deterministic stream | Protocol requires deterministic unbiased recorded permutations; exact vectors absent | Determines event/node/order randomization prospectively | Loop order, rejection threshold, byte order, and vectors become suite identity | Reproducibility owner | Select for Phases 6-7; pass by Phase 16 | New permutation suite/version prospectively only |
| D-013 | Mixing / rolling checksums | SplitMix-style fixed mixer; xxHash-style primitive; cryptographic per-row update; custom | `UNRESOLVED`; no exact formula accepted. Candidate must be fixed-structure, branch-free in the record path, private-state only, and independently vectorized/tested. Artifact/content/index/delta identities use SHA-256 over versioned canonical byte sequences, not the rolling checksum | Protocol fixes one consumer-private checksum update and separate integrity fields but leaves exact primitive open | Exact instructions are part of consumer work, so this is scientifically sensitive and must be frozen before pilot without treatment outcomes | Algorithm ID and canonical input bytes required in every dependent artifact | Queue/consumer owner / data integrity owner / protocol owner | Exact formula for Phase 6; pass by Phase 16 | New formula is a new treatment-era algorithm suite; post-freeze change requires scientific-impact review and possibly amendment |
| D-018 | Affinity / NUMA / pages / frequency / HW-PF | Linux system APIs; external tools; privileged benchmark code | `PROPOSED` interface mapping: `pthread_setaffinity_np`/`sched_setaffinity` plus actual-CPU checks; `mbind`/`set_mempolicy` or libnuma plus `move_pages`/`/proc` residency evidence; base-page/THP controls plus `smaps`; cpufreq sysfs readback; vendor-specific HW-PF operations only through authorized adapter after exact manual/register evidence | Linux documents the candidate APIs; target inventory, permissions, CPU-specific controls, and behavioral probes are absent | These APIs implement placement/state treatments; command alone is never verified state | Linux-only adapter; capability failures block rather than degrade silently | Platform owner / operator | Interface before Stage 3 acceptance; exact mapping/evidence for Phase 9 and Phase 16 | Platform plugin by ADR and full capability/readback suite |
| D-019 | Privilege / stand / custody | Same operator/account; separated OS accounts/storage; offline/encrypted custody | `UNRESOLVED`; measurement must be unprivileged, but named platform operator, validation custodian, enforcement mechanism, storage boundary, recovery, and audit retention are not supplied | Access/sealing protocol requires technical inaccessibility and named roles; repository has no institutional evidence | Required to protect H3 chronology and platform validity | Deployment/account/key/storage design is institution-specific | Repository owner / institution security owner / validation custodian | Architecture names before Stage 3 completion; operational proof by Phase 16; final H3 roles before confirmation | Authority changes append a new custody record; weaker boundary requires protocol review |
| D-020 | Compression / copy policy | None; post-run lossless compression; one or more durable replicas | `UNRESOLVED`; always outside timed path and never credited to hot-buffer capacity | Exact codec, storage budget, durability boundary, and recovery evidence absent | No logical change; timing contamination forbidden | Compression/copy identities and counts recorded; corruption/round-trip gates required | Storage owner | Select for Phase 11; pass by Phase 16 | New artifact encoding/copy policy prospectively; immutable sources retained |
| D-027 | Exponential transform / time units / overflow | Standard-library distribution; specified inverse transform; precomputed schedule tool | `UNRESOLVED`; choose an exact cross-platform algorithm, input-bit mapping, rounding, integer tick unit, overflow rule, and vectors | Standard-library distributions are not promised bit-identical; protocol requires exact reproducibility and half-open horizons | Determines arrival deadlines and therefore realized load; cannot be selected from outcomes | Schedule algorithm suite and decoder must be stable across tools | Reproducibility owner / timing owner | Select for Phase 7; pass by Phase 16 | New schedule suite/version prospectively; old artifacts remain tied to old suite |

## Later protocol-defined decisions with open values

Before confirmatory execution, the protocol still requires treatment-blind values/evidence for `delta_star`; cache-derived capacities/residency maps; rates and `mu_ref`; whole-matrix zero-loss inputs; ring `d2`; horizons/warm-up/moving-block/recovery values; repetitions/bootstrap budget; master/derived seeds and immutable roles; replacement budget/authority; H3 signatures/access; environmental limits/counters; and stand-hours/final block plan. These are Stage 6-8 outputs under the fixed rules, not Stage 2 defaults.

Submission identity, venue, accessibility, archival license, and final metadata remain submission-only decisions.

## Stage 2 disposition

The six architecture decisions above are accepted because the user explicitly required those boundaries. The recommended software, queue, atomic, deterministic, process, platform, generated-code, and sanitizer choices are not accepted. Therefore Stage 2 is documented but **not frozen**, and Stage 3 production implementation remains blocked pending the questions in `docs/DECISIONS_REQUIRED.md`.
