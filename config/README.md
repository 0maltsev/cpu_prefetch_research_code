# Configuration

This directory contains versioned, non-local configuration inputs. Stage 3 adds
only the dependency/license inventory. Experiment plans, platform values, and
scientific configuration do not belong here until their lifecycle decisions are
accepted and their validators exist.

`local/` is ignored for operator-specific configuration. A local file is never
evidence of requested or verified stand state.
